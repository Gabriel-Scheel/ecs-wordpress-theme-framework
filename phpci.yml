build_settings:
        verbose: false
        ignore:
                - "assets"
                - "_cutup"
                - "vendors"
                - "core/Inflector.php"
test:
        php_mess_detector:
                rule: "/phpmd.xml"
                rules: "/phpmd.xml"
                allow_failures: true
        php_code_sniffer:
                standard: "/phpcs.xml"
                args: "-s --report=gitblame --report-summary --report-full"
        php_cpd:
                allow_failures: true

complete:
        email: 
                addresses: 
                        - "devops@noblestudios.com"
